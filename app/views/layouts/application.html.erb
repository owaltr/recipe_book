<!DOCTYPE html>
<html>
  <head>
    <title>RecipeBook</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <%= link_to "Home", root_path, class: class_names("nav-link", { active: current_page?(root_path) }) %>
            <%= link_to "Aktuelles", news_index_path, class: class_names("nav-link", { active: current_page?(news_index_path) }) %>
            <%= link_to "Kategorien", categories_index_path, class: class_names("nav-link", { active: current_page?(categories_index_path) }) %>
            <%= link_to "Impressum", imprint_index_path, class: class_names("nav-link", { active: current_page?(imprint_index_path) }) %>
            <%= link_to "Datenschutz", privacy_index_path, class: class_names("nav-link", { active: current_page?(privacy_index_path) }) %>
          </div>
        </div>
      </div>
    </nav>


    <%= yield %>
  </body>
</html>
